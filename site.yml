site:
  title: Local Preview
  start_page: OpenCPN:install-guide:index
content:
  sources:
   - url: .
     branches: HEAD
   - url: https://github.com/Rasbats/opencpn_docs.git
     #https://pagure.io/fedora-docs/release-docs-home.git
     branches: main
   
ui:
  bundle:
    url: https://github.com/Rasbats/opencpn_plugins_docs/raw/main/ui-bundle.zip
    snapshot: true
  default_layout: with_menu
output:
  clean: true
  dir: ./public
  destinations:
  - provider: archive
runtime:
  pull: true
  cache_dir: ./cache
